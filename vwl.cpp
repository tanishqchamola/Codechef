#include <iostream>
#include <string.h>
using namespace std;
/*
int main()
{
    int n,count=0;
    string str;
    cin>>n>>str;
    
    size_t temp_a = str.find('a');
    if(temp_a<10000000)
    {
        count++;
    }
    
    size_t temp_e = str.find('e');
    if(temp_e<10000000)
    {
        count++;
    }
    
    size_t temp_i = str.find('i');
    if(temp_i<10000000)
    {
        count++;
    }
    
    size_t temp_o = str.find('o');
    if(temp_o<10000000)
    {
        count++;
    }
    
    size_t temp_u = str.find('u');
    if(temp_u<10000000)
    {
        count++;
    }
    
    if(count<4)
    {
        cout<<"No";
    }
    else{
        cout<<"Yes";
    }

    return 0;
}
*/
